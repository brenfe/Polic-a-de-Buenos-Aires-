<%= form_with(model: policeman) do |form| %>
  <% if policeman.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(policeman.errors.count, "error") %> prohibited this policeman from being saved:</h2>

      <ul>
        <% policeman.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :person_id, 'Persona', class: "form-label" %>
    <%= form.collection_select :person_id, Person.all, :id, :nombre_completo, { :include_blank => true }, {class: 'form-select'} %>
  </div>

  <div class="field">
    <%= form.label :police_station_id, 'Comisaría', class: "form-label" %>
    <%= form.collection_select :police_station_id, PoliceStation.all, :id, :name, { :include_blank => true }, {class: 'form-select'} %>
  </div>

  <div class="actions">
    <%= form.submit 'Guardar cambios', class: "btn btn-success" %>
  </div>
<% end %>
