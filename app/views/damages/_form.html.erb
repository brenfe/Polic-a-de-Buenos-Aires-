<%= form_with(model: damage) do |form| %>
  <% if damage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(damage.errors.count, "error") %> prohibited this damage from being saved:</h2>

      <ul>
        <% damage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="field">
    <%= form.label :person_id %>
    <%= form.collection_select :person_id, Person.all, :id, :nombre_completo, html_options = { class: "form-select form-control"} %>
  </div>

  <div class="field">
    <%= form.label :property_id %>
    <%= form.collection_select :property_id, Property.all, :id, :propiedad, html_options = { class: "form-select form-control"} %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
